{
  "name": "mosaic-x",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "typecheck": "tsc -b",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
