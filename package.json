{
  "name": "mosaic-x",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "local": "pnpm --filter @mosaic/local-app start",
    "local:dev": "pnpm --filter @mosaic/local-app dev",
    "vault:recover": "pnpm --filter @mosaic/zone-keys recover",
    "typecheck": "tsc -b",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
