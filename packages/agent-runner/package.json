{
  "name": "@mosaic/agent-runner",
  "version": "0.0.0",
  "description": "Agent Runner process boundary for Mosaic.",
  "type": "module",
  "license": "MIT",
  "bin": { "mosaic-agent-runner": "./dist/bin.js" },
  "files": ["dist"],
  "exports": { "./package.json": "./package.json" },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "start": "node dist/bin.js",
    "test": "tsc -b && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@mosaic/local-runtime": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "typescript": "~6.0.2"
  }
}
