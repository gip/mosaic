{
  "name": "@mosaic/local-runtime",
  "version": "0.0.0",
  "description": "Shared lifecycle and IPC contract for Mosaic local utility processes.",
  "type": "module",
  "license": "MIT",
  "files": ["dist"],
  "exports": { ".": "./dist/index.js", "./contracts": "./dist/contracts.js" },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "tsc -b && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "typescript": "~6.0.2"
  }
}
