{
  "name": "@mosaic/xrpl",
  "version": "0.0.0",
  "description": "XRPL market data (CLOB order books, path_find quote surfaces) and trusted-asset balances",
  "license": "MIT",
  "type": "module",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22"
  },
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "tsc -b && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@mosaic/chain-core": "workspace:*",
    "xrpl": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "typescript": "~6.0.2"
  }
}
