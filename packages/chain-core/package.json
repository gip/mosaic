{
  "name": "@mosaic/chain-core",
  "version": "0.0.0",
  "description": "Chain-agnostic market-data and balance interfaces + feed lifecycle shared by @mosaic/xrpl, @mosaic/stellar, @mosaic/evm",
  "license": "MIT",
  "type": "module",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22"
  },
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "tsc -b && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "typescript": "~6.0.2"
  }
}
